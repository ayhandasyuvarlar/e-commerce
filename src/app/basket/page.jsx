import React from "react";
import { metadata } from "./layout";

const Basket = () => {
  const lenght = 0;
  metadata.title.default = `(${lenght})  Basket `;

  return <div>Basket</div>;
};

export default Basket;
